import styled from 'styled-components';
import { theme } from '../../global.styles';

export const ToggleTourContainer = styled.div`
	margin: 20px;
	display: flex;
	padding: 20px;
	background-color: ${theme.secondaryLight};
	color: ${theme.background};
	border-radius: 5px;
`;
